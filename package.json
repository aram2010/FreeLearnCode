{
  "name": "freelearncode",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": "^20.19.0 || >=22.12.0"
  },
  "scripts": {
  // 🚀 Запуск проекта в режиме разработки (http://localhost:5173)
  "dev": "vite",

  // 🧩 То же самое — просто альтернативные названия
  "serve": "vite",
  "start": "vite",

  // 🏗️ Сборка проекта для продакшена (результат в папке dist)
  "build": "vite build",

  // 👀 Локальный предпросмотр собранного билда
  "preview": "vite preview",

  // 🔍 Проверка кода на ошибки и стиль (ESLint)
  "lint": "eslint . --ext .js,.vue",

  // 🛠️ Исправление простых ошибок ESLint автоматически
  "lint:fix": "eslint . --ext .js,.vue --fix",

  // 🎨 Автоматическое форматирование кода (Prettier)
  "format": "prettier --write 'src/**/*.{js,vue,css,scss,json}'",

  // 🧹 Очистка старой сборки (удаляет папку dist)
  "clean": "rimraf dist",

  // 📊 Анализ сборки (работает, если установлен vite-plugin-visualizer)
  "analyze": "vite build --mode analyze",

  // 🌍 Сборка и автоматический деплой на GitHub Pages
  "deploy:gh": "npm run build && gh-pages -d dist"
}
  "dependencies": {
    "vue": "^3.5.22"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^6.0.1",
    "vite": "^7.1.12",
    "vite-plugin-vue-devtools": "^8.0.3"
  }
}
